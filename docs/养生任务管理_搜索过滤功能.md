# 养生任务管理 - 搜索过滤功能

## 新增功能

### 1. 境界关联显示
- ✅ 境界要求现在显示实际的境界名称（如"胎息境"、"聚灵境"）
- ✅ 境界范围显示为"胎息境 - 玉骨境"而不是"1-5"
- ✅ 表单中使用下拉选择境界，而不是手动输入数字

### 2. 搜索功能
- ✅ 支持搜索任务名称
- ✅ 支持搜索任务键名（task_key）
- ✅ 支持搜索任务描述
- ✅ 实时搜索，无需点击按钮

### 3. 境界过滤
- ✅ 可以按境界过滤任务
- ✅ 显示该境界可用的所有任务
- ✅ 下拉列表显示所有境界名称

### 4. 统计信息
- ✅ 显示"显示 X / Y 个任务"
- ✅ 实时更新过滤结果数量

### 5. 清除筛选
- ✅ 一键清除所有搜索和过滤条件
- ✅ 只在有筛选条件时显示

## 使用方法

### 养生任务配置页面

#### 搜索任务
1. 在搜索框中输入关键词
2. 系统会自动搜索任务名称、键名和描述
3. 结果实时更新

**示例：**
- 搜索"子午" → 找到"子午流注"
- 搜索"ziwu" → 找到task_key为"ziwu"的任务
- 搜索"早睡" → 找到描述中包含"早睡"的任务

#### 按境界过滤
1. 点击"境界过滤"下拉框
2. 选择一个境界（如"胎息境"）
3. 显示该境界可用的所有任务

**示例：**
- 选择"胎息境" → 显示8个任务（6个基础+2个专属）
- 选择"聚灵境" → 显示2个超凡期任务

#### 组合使用
可以同时使用搜索和境界过滤：
- 搜索"吐纳" + 境界"胎息境" → 找到"胎息吐纳"

#### 清除筛选
点击"清除筛选"按钮，恢复显示所有任务

### 创建/编辑任务

#### 选择境界
1. **境界ID**：任务主要所属的境界
2. **最小境界**：任务可用的最低境界
3. **最大境界**：任务可用的最高境界

**示例配置：**
- 基础任务：境界ID=1，最小=1，最大=5（所有凡蜕期可用）
- 专属任务：境界ID=1，最小=1，最大=1（仅胎息境可用）
- 超凡任务：境界ID=6，最小=6，最大=6（仅聚灵境可用）

### 扩展修炼项目页面

#### 搜索项目
- 在搜索框中输入关键词
- 搜索项目名称、键名或描述
- 结果实时更新

## 界面预览

### 搜索和过滤栏
```
┌─────────────────────────────────────────────────────────────┐
│ 🔍 [搜索任务名称、键名或描述...]  境界过滤: [全部境界 ▼]  │
│                                   [✕ 清除筛选]  显示 8/24   │
└─────────────────────────────────────────────────────────────┘
```

### 任务列表显示
```
┌─────────────────────────────────────────────────────────────┐
│ 🌙 子午流注 (ziwu)                              [编辑] [删除] │
│ 23:00前入睡，顺应天地阴阳，调和气血                          │
│ ⏳ 寿命: 30分钟  💎 功德: 2  类型: 固定  境界要求: 基础      │
│ 🏔️ 胎息境 - 玉骨境  排序: 1                                 │
└─────────────────────────────────────────────────────────────┘
```

### 表单中的境界选择
```
┌─────────────────────────────────────────────────────────────┐
│ 境界ID *                                                     │
│ [胎息境 ▼]                                                   │
│                                                              │
│ 最小境界 *                                                   │
│ [胎息境 ▼]                                                   │
│                                                              │
│ 最大境界 *                                                   │
│ [玉骨境 ▼]                                                   │
└─────────────────────────────────────────────────────────────┘
```

## 技术实现

### 数据加载
```typescript
// 加载境界列表
async function loadRealms() {
  const res = await api('/realms/list')
  realms.value = res.realms || []
}
```

### 搜索过滤
```typescript
const filteredWellnessTasks = computed(() => {
  let filtered = wellnessTasks.value

  // 搜索过滤
  if (searchQuery.value.trim()) {
    const query = searchQuery.value.toLowerCase()
    filtered = filtered.filter(task => 
      task.name.toLowerCase().includes(query) ||
      task.task_key.toLowerCase().includes(query) ||
      task.description.toLowerCase().includes(query)
    )
  }

  // 境界过滤
  if (filterRealmId.value !== null) {
    filtered = filtered.filter(task => 
      task.min_realm_id <= filterRealmId.value! && 
      task.max_realm_id >= filterRealmId.value!
    )
  }

  return filtered
})
```

### 境界名称显示
```typescript
// 获取境界名称
const getRealmName = (realmId: number): string => {
  const realm = realms.value.find(r => r.id === realmId)
  return realm?.name || `境界${realmId}`
}

// 获取境界范围描述
const getRealmRangeDesc = (minRealmId: number, maxRealmId: number): string => {
  if (minRealmId === maxRealmId) {
    return getRealmName(minRealmId)
  }
  return `${getRealmName(minRealmId)} - ${getRealmName(maxRealmId)}`
}
```

## 样式特点

### 搜索框
- 左侧搜索图标
- 半透明背景
- 聚焦时高亮边框
- 占位符提示

### 过滤下拉框
- 金色边框主题
- 悬停效果
- 清晰的选项列表

### 清除按钮
- 红色主题
- 只在有筛选时显示
- 悬停效果

### 统计信息
- 青色文字
- 右对齐显示
- 实时更新

## 性能优化

### 计算属性
- 使用Vue的computed自动缓存
- 只在依赖变化时重新计算
- 避免不必要的渲染

### 搜索优化
- 实时搜索，无需防抖（数据量小）
- 大小写不敏感
- 支持部分匹配

### 分页
- 扩展任务支持分页
- 每页10条记录
- 过滤后自动更新总数

## 用户体验

### 即时反馈
- 输入即搜索
- 无需点击按钮
- 结果立即显示

### 清晰提示
- 空状态提示
- 统计信息显示
- 占位符引导

### 灵活组合
- 可单独使用搜索
- 可单独使用过滤
- 可组合使用
- 可一键清除

## 常见使用场景

### 场景1：查找特定任务
**需求：** 找到"子午流注"任务进行编辑
**操作：** 在搜索框输入"子午"
**结果：** 立即显示"子午流注"任务

### 场景2：查看某境界的所有任务
**需求：** 查看胎息境有哪些任务
**操作：** 境界过滤选择"胎息境"
**结果：** 显示8个任务（6个基础+2个专属）

### 场景3：查找某类型任务
**需求：** 找到所有与"吐纳"相关的任务
**操作：** 搜索框输入"吐纳"
**结果：** 显示"胎息吐纳"等相关任务

### 场景4：验证任务配置
**需求：** 确认超凡期有哪些任务
**操作：** 依次选择聚灵境、神识境、玄府境、凝丹境
**结果：** 每个境界显示2个专属任务

## 未来改进建议

### 高级过滤
- [ ] 按奖励类型过滤（固定/可变/输入）
- [ ] 按奖励数值范围过滤
- [ ] 按启用状态过滤

### 批量操作
- [ ] 批量启用/禁用
- [ ] 批量修改境界范围
- [ ] 批量删除

### 排序功能
- [ ] 按名称排序
- [ ] 按境界排序
- [ ] 按奖励排序
- [ ] 按创建时间排序

### 导出功能
- [ ] 导出为CSV
- [ ] 导出为JSON
- [ ] 导出筛选结果

---

**完成时间：** 2026-05-14  
**状态：** ✅ 已完成并可用
